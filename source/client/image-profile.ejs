<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Perfil de foto</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <% include partials/head%>
    </head>
    <body class="signup">
            <div calss="container p-4">
                <div class="row">
                    <div class="col-md-4 offset-md-4 mt-5 animated fadeInUp">
                        <div class="card">
                                <img src="<%= image.path %>" class="card-img-top">
                                <div class="card-body">
                                    <h5 class="card-title">Mascota: <%=image.pet %></h5>
                                    <p class="card-text">Raza: <%= image.race %></p>
                                    <p class="card-text">Encontrado en <%=image.place %></p>
                                    <p class="card-text">Descripcion: <%= image.description %></p>
                                    <p class="card-text">Contacto: <%= image.contact %></p>
                                    <p class="card-text"><%= format(image.created_at)%></p>
                                    
                                    <%if (loggedUser) { %>
    
                                    <a href="/delete/<%= image._id %>" class="btn btn-danger">
                                    Eliminar
                                    </a>
                                    <a href="/edit/<%= image._id %>" class="btn btn-info">Editar información</a>
                                    </div>
                                    
                                    <% } %>
                            </div>
                                  
                                    <a href="/home" class="btn btn-success mt-5 btn-block">Volver</a>
                                    
                        </div>
                    </div>
                </div>
            </body>
</html>
